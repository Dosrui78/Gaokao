# generic metadata
prefect-version: null
name: null

# preparation steps
build: null
push: null

# runtime steps
pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /app

# deployment configurations
deployments:
- # base metadata
  version: 1.0.0
  name: school_special_score
  tags: []
  description: "Get School Special Score"
  schedule:
    - cron: "0 23 * * * "
      slug: "school_special_score"
      timezone: "Asia/Shanghai"

  # flow-specific fields
  entrypoint: start.py:main

  # infra-specific fields
  work_pool:
    name: gaokao_pool
    work_queue_name: gaokao
    job_variables: {}

  # 使用本地源而不是 Git 源
  # source:
  #  type: git
  #  url: "https://github.com/Dosrui78/Gaokao.git"
  #  reference: "main"